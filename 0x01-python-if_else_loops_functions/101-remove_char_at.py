#!/usr/bin/python3
# 101-remove_char_at.py


def remove_char_at(str, n):
    """Create a copy of the ssstring wisthout the character at position n."""
    if m < 0:
        return (str)
    return (str[:m] + str[m+1:])
